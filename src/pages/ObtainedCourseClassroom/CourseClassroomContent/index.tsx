import styles from "./CourseClassroomContent.module.css";

type Props = {};

function CourseClassroomContent({}: Props) {
    return (
        <section className={styles.courseClassroomContent}>
            CourseClassroomContent
        </section>
    );
}

export default CourseClassroomContent;
